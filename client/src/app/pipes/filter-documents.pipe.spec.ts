import { FilterDocumentsPipe } from './filter-documents.pipe';

describe('FilterDocumentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDocumentsPipe();
    expect(pipe).toBeTruthy();
  });
});
